#  模組介紹：


模組的作用是管理 檔案內程式碼的權限 （類似Spring Boot 一個命名空間有誰能用）

import {
  要將其他模組內的程式碼導入近來要用：      imports [...],
 
  自身模組如果是一個 路由 要用掛載同層級的  controller:[...],
  
  自身模組內的程式碼 要共用共享要用：       providers: [...],

  要將一個模組內的程式碼共用出去要用：      exports: [...],


}